Вопросы:
1) в таких случаях делается одна общая база данных, или для каждого микросервиса своя отдельная(МС)?
2) почему то флайэвей отрабатывает создание БД, но не происходит инсерт... или это гибернейт создает БД таблицы
по-умолчанию, а флайэвей вообще не отрабатывает?
3) не понял, как лучше оформить архитектуру: в заказах я храню только сам заказ, список id товаров, количество товаров
и цену при оформлении. При получении списка заказов мне нужно отображать название товара, для этого надо получить по
списку id в МС product-server /list_products список ProductDto для каждого заказа? И потом на основании этих дто
сформировать список ордерИтемДто для ордерДто?
4) не получается через restTemplate в МС  order-server получить список товаров по заданому списку id. Как передать список?
только через параметры в uri?